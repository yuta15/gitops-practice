apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: application-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/yuta15/gitops-practice.git
    targetRevision: main
    path: manifests/dev/

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true